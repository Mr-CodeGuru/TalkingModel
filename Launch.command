#!/bin/bash
# TalkingModel — Launch.command (macOS double-click launcher)
# This file redirects to the new cross-platform launch script.
# For direct terminal use, prefer: bash scripts/launch.sh

cd "$(dirname "$0")"
bash scripts/launch.sh
